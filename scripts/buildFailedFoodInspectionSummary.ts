import { readFile, writeFile } from 'node:fs/promises';
import type { FailedFoodInspectionRecord, FailedFoodInspectionSummary } from '../src/types';
import { readReport, writeReport } from './dataPipeline';
const records = JSON.parse(await readFile('public/data/failed-food-inspection-records/records.json', 'utf8')) as FailedFoodInspectionRecord[];
const count = (items: string[]) => [...items.reduce((m, item) => m.set(item, (m.get(item) ?? 0) + 1), new Map<string, number>()).entries()].map(([label, count]) => ({ label, count })).sort((a, b) => b.count - a.count || a.label.localeCompare(b.label, 'zh-Hant'));
const byYear = count(records.flatMap((r) => r.inspectionYear ? [String(r.inspectionYear)] : [])).map(({ label, count }) => ({ year: Number(label), count })).sort((a, b) => a.year - b.year);
const byCategory = count(records.flatMap((r) => r.category ? [r.category] : [])).map(({ label: category, count }) => ({ category, count })); const byReason = count(records.flatMap((r) => r.noncomplianceReason ? [r.noncomplianceReason] : [])).map(({ label: reason, count }) => ({ reason, count })); const byDistrict = count(records.flatMap((r) => r.districtName ? [r.districtName] : [])).map(({ label: district, count }) => ({ district, count }));
const report = await readReport(); const quality = report.failedFoodInspectionRecords?.dataQuality ?? { invalidDateCount: 0, duplicateRowCount: 0, missingLocationCount: 0, unknownCategoryCount: 0, unresolvedDistrictCount: 0 };
const summary: FailedFoodInspectionSummary = { generatedAt: new Date().toISOString(), totalRecords: records.length, latestInspectionDate: records.map((r) => r.inspectionDate).filter((v): v is string => Boolean(v)).sort().at(-1), yearsCovered: byYear.length, foodCategoryCount: byCategory.length, uniqueSampleNames: new Set(records.map((r) => r.sampleName).filter(Boolean)).size, districtCount: byDistrict.length, topNoncomplianceReason: byReason[0]?.reason, topCategory: byCategory[0]?.category, byYear, byMonth: count(records.flatMap((r) => r.inspectionDate ? [r.inspectionDate.slice(0, 7)] : [])).map(({ label: month, count }) => ({ month, count })), byCategory, byNoncomplianceReason: byReason, byDistrict, byCategoryAndYear: count(records.flatMap((r) => r.category && r.inspectionYear ? [`${r.category}\u0000${r.inspectionYear}`] : [])).map(({ label, count }) => { const [category, year] = label.split('\u0000'); return { category, year: Number(year), count }; }), dataQuality: quality };
await writeFile('public/data/failed-food-inspection-records/summary.json', `${JSON.stringify(summary, null, 2)}\n`); if (report.failedFoodInspectionRecords) await writeReport({ ...report, failedFoodInspectionRecords: { ...report.failedFoodInspectionRecords, ...summary } }); console.log(`Built failed-food-inspection summary for ${records.length} records.`);
